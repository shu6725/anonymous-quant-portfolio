---
interface Props {
  label: string;
  value: string;
  detail?: string;
}

const { label, value, detail } = Astro.props;
---

<aside class="metric-callout" aria-label={label}>
  <span>{label}</span>
  <strong>{value}</strong>
  {detail && <small>{detail}</small>}
</aside>
